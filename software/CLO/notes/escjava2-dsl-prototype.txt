NAME::

  [escj | escjava2] description "Extended Static Checking for Java, Version 2"

ARGS::

 [-warn <category> | -nowarn <category>] multiplicity "*" description "Which types of warnings to show"
 [-simplify <pathname> | -useInternal] description "Which version i.e. platform of Simplify theorem prover to use"
 [-javaVersion <version>] description "Which version of JDK specifications in JML to use"
 [-noCheck | -typecheck]
 [-classpath]  //>>> needs an arg
 [-PrettyPrintVC | -PrintVC]
 [-eaJML | -eaJava]
 [-prover <prover> | -svcg <prover> | -NoCheck]
 [-noTrace | -VerboseTrace]
 [-Loop <positive integer|half-integer> | -LoopSafe | -LoopFallThru] //>>> what does | mean? these override one another whereas warn nowarn are more complicated
 [-Routine <routine> | -RoutineIndirect? <filename>]
 [-idc -erst]
 [-inlineConstructors | -inlineCheckDepth | -inlineDepthPastCheck | -inlineCheckDepth -inlineDepthPastCheck] 
 [-CounterExample] description "Show a counter example for each warning"
 [-NoSemicolonWarnings]
 [-Simplify <pathname>]
 [-Specs <pathname | filename>] description "Where to find the JML specifications for the Java JDK"
    * NoRedundancy
    * nonNullByDefault
    * NoWarn
    * PlainWarning
    * Start <positive integer>
    * Stats
    * Suggest
    * VCLimit <positive integer>
    * Warn <category> 
 
 <filename>

ARGS_FORMAT::

 [-warn <flag:enum>]{*} //>>> how does this relate to the preceeding declarations?
 [-classpath <cp:string>]
 [-simplify <path:string>]

WHERE::

 <flag:enum> = ZeroDiv | ArrayStore | Assert | Cast | Reachable \
        | Inconsistent | Constraint | CLeak | DecreasesBound \
        | Decreases | Unreadable | Undefined | UndefinedNormalPost \
        | UndefinedExceptionalPost | IndexNegative | IndexTooBig \
        | Uninit | ILeak | Initially | Deadlock | LoopInv | LoopObjInv \
        | ModExt | Modifies | NegSize | NonNull | NonNullInit | Null \
        | Invariant | OwnerNull | Post | Pre | Race | RaceAllNull \
        | Unenforcable | Exception | SpecificationException \
        | Deferred | Writable

DEFAULTS::

SPECIFIED_BY_DEFAULT::

OVERRIDES::

 [-nowarn <flag:enum>] overrides [-warn <flag:enum>] //>>> here you need the value on the left hand side, interesting, maybe use something linke \1 in regex
 [-useInternal] overrides [-simplify]

VALIDITY::

DOCS::

NOTES::

 https://mobius.ucd.ie/trac/wiki/ESCCLI